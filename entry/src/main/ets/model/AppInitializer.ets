// model/AppInitializer.ets
export function initAppData(): void {
  // Simular carga pesada
  /*let sum = 0;
  for (let i = 0; i < 1e8; i++) {
    sum += i;
  }*/
  console.info('Datos inicializados');
}